package com.shawnclake.morgencore.core.component.commands;

import com.shawnclake.morgencore.core.component.events.Listener;
import com.shawnclake.morgencore.core.component.interactables.InteractableInputEvent;
import com.shawnclake.morgencore.core.component.terminal.Output;

public class CommandEventListener implements Listener {

    public void onInteractableInput(InteractableInputEvent e)
    {
        //Output.pln("srhg");

        //Output.pln(e.getMessage().get());
    }

}
